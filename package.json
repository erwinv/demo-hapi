{
  "name": "demo-hapi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start -w backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
